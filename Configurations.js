let rows_max = 10
let cols_max = 10
let wall = false
let difficulty = undefined

function startGame(start){
        if (start.code == "Enter") {
            game.started = true
            setInterval(defaultBehavior, 500)
        }
    return
}